.env
.runtime/
__pycache__/
.pytest_cache/
venv/
venv_herald/
*.pkl
cache_directory/
herald/cache/
client_secret.json
nodes.json
wandb/
*.egg-info/
build/
.cursor/
.dev_planning/
.vscode
historical_videos.jsonl
terraform/

.venv/
cache/

# Operator signing keys must never enter source control.
*.ed25519.key
herald_registry.key
herald_briefs.key
brief_feed.key

# internal ops/design docs — keep local, never on GitHub. AGENTS.md stays at the repo root
# (Codex auto-loads it there, same as CLAUDE.md); everything else lives under dev/.
AGENTS.md
dev/*.md
